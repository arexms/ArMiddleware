#pragma once

namespace Ar {
    namespace Middleware
    {
        enum class MessageId
        {
            BASE                        = 0,
            LAMBDA_MESSAGE              = 1,
            TEST_MESSAGE                = 2,
            TEST_MESSAGE_RESPONSE       = 3,
            END
        };
    }
}

